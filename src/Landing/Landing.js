import React from 'react';
import './Landing.scss';

const Landing = () => (
    <h2>This simple todo application is a task-management tool.
      It allows users to manage tasks from a computer or smartphone.
      This application contains a fixed side navigation menu,
      the use of the SASS (a css pre-processing tool) to create a basic layered paper effect, and
      an animated modal (dialog box) that is hidden by default.
      The implementation of React JS, when combined with a renderer,
      captures data and responds to user inputs.</h2>
);
export default Landing;
