import React from 'react';
import './Landing.scss';

const Landing = () => (
    <h2>This todo application is a simple task-management tool.
      This application contains a fixed side navigation menu with image animation
          and an animated modal (dialog box), which is hidden by default.
      The use of SASS (a css pre-processing tool) is used to create a basic layered paper effect;
          and, the implementation of React JS, when combined with a renderer,
          captures data and responds to user inputs.</h2>
);
export default Landing;
